# Herdr keymap shared by macOS, Linux, Windows, and devcontainers.
# Mirrors Omarchy's tmux/Herdr muscle memory. Keep this file portable:
# host-specific shells and launch commands belong in host configuration.
#
# Omarchy reference: https://omarchy.org/manual/hotkeys/#tmux
# Herdr reference:   https://herdr.dev/docs/configuration/

[session]
resume_agents_on_restore = true

[keys]
prefix = "ctrl+space"

# General
help = "prefix+?"
settings = ""
detach = "prefix+d"
reload_config = "prefix+q"
workspace_picker = "prefix+s"

# Workspaces (Omarchy tmux sessions)
new_workspace = "prefix+shift+c"
rename_workspace = "prefix+shift+r"
close_workspace = "prefix+shift+k"
previous_workspace = ["prefix+shift+p", "alt+up"]
next_workspace = ["prefix+shift+n", "alt+down"]

# Tabs (Omarchy tmux windows)
new_tab = "prefix+c"
rename_tab = "prefix+r"
close_tab = "prefix+k"
previous_tab = "alt+left"
next_tab = "alt+right"
move_tab_previous = "alt+shift+left"
move_tab_next = "alt+shift+right"
switch_tab = "alt+1..9"

# Panes
split_vertical = ["prefix+v", "alt+shift+enter"]
split_horizontal = ["prefix+h", "alt+enter"]
close_pane = ["prefix+x", "alt+esc"]
zoom = "prefix+z"
copy_mode = "prefix+["
resize_mode = ""

focus_pane_left = "ctrl+alt+left"
focus_pane_down = "ctrl+alt+down"
focus_pane_up = "ctrl+alt+up"
focus_pane_right = "ctrl+alt+right"

resize_pane_left = "ctrl+alt+shift+left"
resize_pane_down = "ctrl+alt+shift+down"
resize_pane_up = "ctrl+alt+shift+up"
resize_pane_right = "ctrl+alt+shift+right"
